Научимся определять расстояние между точками. Далее, зная, что время — это расстояние делить на скорость,
будем определять время для каждого таксиста, за которое он сможет доехать до Василия. 
Ответ это минимальное из времен.Не забудем про double.

#include<bits/stdc++.h>
using namespace std;
int n;
double a,b,x,y,v,mn=400;
int main()
{
	cin>>a>>b>>n;
	for(int i=0;i<n;i++)
	{
		cin>>x>>y>>v;
		mn=min(mn,(sqrt((x-a)*(x-a)+(y-b)*(y-b)))/v);
	}
	cout<<setprecision(20);
	cout<<mn;
	return 0;
}
